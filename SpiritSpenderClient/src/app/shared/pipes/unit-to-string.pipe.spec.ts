import { UnitToStringPipe } from './unit-to-string.pipe';

describe('UnitToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new UnitToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
